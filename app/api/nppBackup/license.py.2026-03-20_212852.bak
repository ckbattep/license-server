from fastapi import APIRouter
from app.security.hmac_utils import sign_response
import json

router = APIRouter()

@router.post("/license/status")
def status():
    data = {
        "status": "active"
    }

    payload = json.dumps(data)
    return sign_response(payload)